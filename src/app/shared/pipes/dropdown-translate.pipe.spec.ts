import { DropdownTranslatePipe } from './dropdown-translate.pipe';

describe('DropdownTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
